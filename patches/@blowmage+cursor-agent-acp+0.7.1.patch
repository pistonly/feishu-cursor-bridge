diff --git a/node_modules/@blowmage/cursor-agent-acp/dist/adapter/cursor-agent-adapter.js b/node_modules/@blowmage/cursor-agent-acp/dist/adapter/cursor-agent-adapter.js
index 686283b..68f8cc9 100644
--- a/node_modules/@blowmage/cursor-agent-acp/dist/adapter/cursor-agent-adapter.js
+++ b/node_modules/@blowmage/cursor-agent-acp/dist/adapter/cursor-agent-adapter.js
@@ -641,8 +641,23 @@ class CursorAgentAdapter {
         // Validate metadata before creating session
         this.validateSessionMetadata(metadata);
         // Create cursor-agent chat for this session
-        let cursorChatId;
-        if (this.cursorBridge) {
+        const metaCursorChatId = typeof params._meta?.cursorChatId === 'string' && params._meta.cursorChatId.length > 0
+            ? params._meta.cursorChatId
+            : undefined;
+        let cursorChatId = typeof metadata.cursorChatId === 'string' && metadata.cursorChatId.length > 0
+            ? metadata.cursorChatId
+            : metaCursorChatId;
+        if (metaCursorChatId && !metadata.cursorChatId) {
+            this.logger.debug('Using cursor-agent chat from ACP _meta for session', {
+                chatId: metaCursorChatId,
+            });
+        }
+        if (cursorChatId) {
+            this.logger.debug('Using provided cursor-agent chat for session', {
+                chatId: cursorChatId,
+            });
+        }
+        else if (this.cursorBridge) {
             try {
                 cursorChatId = await this.cursorBridge.createChat();
                 this.logger.debug('Created cursor-agent chat for session', {
@@ -689,6 +704,7 @@ class CursorAgentAdapter {
                 createdAt: sessionData.createdAt.toISOString(),
                 cwd,
                 mcpServerCount: mcpServers.length,
+                ...(cursorChatId && { cursorChatId }),
                 ...(mcpServers.length > 0 && {
                     mcpStatus: 'not-implemented',
                     mcpServers: mcpServers.map((s) => ({
diff --git a/node_modules/@blowmage/cursor-agent-acp/dist/cursor/cli-bridge.js b/node_modules/@blowmage/cursor-agent-acp/dist/cursor/cli-bridge.js
index 2926b57..cd75833 100644
--- a/node_modules/@blowmage/cursor-agent-acp/dist/cursor/cli-bridge.js
+++ b/node_modules/@blowmage/cursor-agent-acp/dist/cursor/cli-bridge.js
@@ -442,6 +442,54 @@ class CursorCliBridge {
     generateSessionId() {
         return `session-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
     }
+    normalizeStreamingAssistantText(text, state) {
+        if (typeof text !== 'string' || text.length === 0) {
+            return '';
+        }
+        const previousText = state.assistantText || '';
+        if (!previousText) {
+            state.assistantText = text;
+            return text;
+        }
+        // Newer cursor-agent stream-json emits text deltas first and may end with a
+        // final full assistant snapshot. Suppress the duplicate snapshot while still
+        // allowing cumulative snapshots to contribute only their unseen suffix.
+        if (text === previousText) {
+            return '';
+        }
+        if (text.startsWith(previousText)) {
+            const delta = text.slice(previousText.length);
+            state.assistantText = text;
+            return delta;
+        }
+        state.assistantText += text;
+        return text;
+    }
+    extractStreamingContentBlocks(event, state) {
+        if (!event || typeof event !== 'object') {
+            return [];
+        }
+        if (event.type !== 'assistant') {
+            return [];
+        }
+        const blocks = event.message?.content;
+        if (!Array.isArray(blocks) || blocks.length === 0) {
+            return [];
+        }
+        if (blocks.length === 1 &&
+            blocks[0]?.type === 'text' &&
+            typeof blocks[0].text === 'string') {
+            const text = this.normalizeStreamingAssistantText(blocks[0].text, state);
+            if (!text) {
+                return [];
+            }
+            return [{
+                    ...blocks[0],
+                    text,
+                }];
+        }
+        return blocks;
+    }
     /**
      * Send a prompt to Cursor CLI
      */
@@ -607,45 +655,70 @@ class CursorCliBridge {
             }
             let responseContent = '';
             let processedChunks = 0;
-            // Execute streaming command
-            const streamOptions = {
-                cwd: workingDir,
-                onData: async (chunk) => {
-                    processedChunks++;
-                    responseContent += chunk;
-                    // Try to parse as JSON stream
-                    try {
-                        const jsonChunk = JSON.parse(chunk);
-                        if (onChunk) {
-                            await onChunk({
-                                type: 'content',
-                                data: jsonChunk,
-                            });
-                        }
-                    }
-                    catch {
-                        // If not JSON, send as plain text
-                        if (onChunk) {
+            let streamBuffer = '';
+            const streamState = {
+                assistantText: '',
+            };
+            const processStreamLine = async (rawLine) => {
+                const line = rawLine.trim();
+                if (!line) {
+                    return;
+                }
+                processedChunks++;
+                try {
+                    const jsonChunk = JSON.parse(line);
+                    const contentBlocks = this.extractStreamingContentBlocks(jsonChunk, streamState);
+                    if (onChunk) {
+                        for (const contentBlock of contentBlocks) {
                             await onChunk({
                                 type: 'content',
-                                data: chunk,
+                                data: contentBlock,
                             });
                         }
                     }
-                    if (onProgress) {
-                        onProgress({
-                            step: 'streaming',
-                            current: processedChunks,
-                            progress: processedChunks,
-                            message: `Received chunk ${processedChunks}`,
+                }
+                catch (error) {
+                    this.logger.debug('Failed to parse stream-json line, forwarding as text', {
+                        error: error instanceof Error ? error.message : String(error),
+                        linePreview: line.substring(0, 160),
+                    });
+                    if (onChunk) {
+                        await onChunk({
+                            type: 'content',
+                            data: line,
                         });
                     }
+                }
+                if (onProgress) {
+                    onProgress({
+                        step: 'streaming',
+                        current: processedChunks,
+                        progress: processedChunks,
+                        message: `Received chunk ${processedChunks}`,
+                    });
+                }
+            };
+            // Execute streaming command
+            const streamOptions = {
+                cwd: workingDir,
+                onData: async (chunk) => {
+                    responseContent += chunk;
+                    streamBuffer += chunk;
+                    const lines = streamBuffer.split(/\r?\n/);
+                    streamBuffer = lines.pop() || '';
+                    for (const line of lines) {
+                        await processStreamLine(line);
+                    }
                 },
             };
             if (abortSignal) {
                 streamOptions.abortSignal = abortSignal;
             }
             const response = await this.executeStreamingCommand(args, streamOptions);
+            if (streamBuffer.trim()) {
+                await processStreamLine(streamBuffer);
+                streamBuffer = '';
+            }
             // Send completion chunk
             if (onChunk) {
                 await onChunk({
diff --git a/node_modules/@blowmage/cursor-agent-acp/dist/protocol/prompt.js b/node_modules/@blowmage/cursor-agent-acp/dist/protocol/prompt.js
index 2db769a..cd3dfec 100644
--- a/node_modules/@blowmage/cursor-agent-acp/dist/protocol/prompt.js
+++ b/node_modules/@blowmage/cursor-agent-acp/dist/protocol/prompt.js
@@ -1308,6 +1308,9 @@ class PromptHandler {
         // Per ACP schema: field is 'prompt', not 'content'
         // Support both for backward compatibility but prefer 'prompt'
         const { sessionId, prompt, content, stream, metadata } = params;
+        const metaStream = typeof params?._meta?.stream === 'boolean'
+            ? params._meta.stream
+            : undefined;
         if (!sessionId || typeof sessionId !== 'string') {
             throw new types_1.ProtocolError('sessionId is required and must be a string');
         }
@@ -1325,7 +1328,7 @@ class PromptHandler {
         return {
             sessionId,
             content: contentArray, // Normalize to 'content' internally for processing
-            stream: Boolean(stream),
+            stream: Boolean(stream ?? metaStream),
             metadata: metadata || {},
         };
     }
