---
description: reference/ 与 memory/ 为本地目录，不纳入 Git 版本库
alwaysApply: true
---

# 本地目录：不提交到 Git

本仓库约定以下目录仅存在于开发者本机，**不要**加入 Git（已在 `.gitignore` 中忽略）。

## `reference/`

- **用途**：放置第三方或本地的参考代码、文档、压测样例等，便于对照实现或调试。
- **为何不提交**：体积通常较大、来源多样，且非本仓库的「正式源码」；克隆仓库的人可按需自行放置资料。

## `memory/`

- **用途**：项目记忆文件（如开发背景、调试结论、会话沉淀），供 Agent 或本人查阅。
- **为何不提交**：往往含环境路径、个人笔记或与具体机器相关的内容，不适合作为共享的单一事实来源进入版本历史。

## 对 Agent 的指引

- 修改代码、写提交说明、做 PR 时，**不要**把 `reference/` 或 `memory/` 下的改动算进待提交范围；若用户要求提交这些目录，应提醒其违反本仓库约定。
- 需要引用参考实现时，可在回复中说明路径在本地 `reference/` 下，但不要假设远端仓库里存在相同文件。
